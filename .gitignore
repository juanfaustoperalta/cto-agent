# Marketplace skills linkeadas runtime por bin/install-agent (HUD-431).
# El runtime las regenera en cada install/upgrade — no son contenido del repo.
.claude/skills/
.claude/settings.local.json

# Plans generados por la skill `writing-plans` (superpowers).
# Carlos ya levanta planes inline; estos archivos son basura local.
docs/plans/

# macOS cruft
.DS_Store

# Claude Code settings inyectados por bin/install-agent (HUD-480-A).
.claude/settings.json
